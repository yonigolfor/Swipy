<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Normal (not runtime-dangerous) permission — required for HapticManager's
         Vibrator.vibrate() calls; missing this throws a SecurityException at the first swipe,
         not a silent no-op (found via a real on-device crash, not caught by any build/test
         step — vibrator.hasVibrator() only checks hardware capability, not this permission). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Runtime-requested on API 33+ (see :core:notifications' permission-request TODO) —
         notifications work without it below 33, matching the platform's own behavior. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Not USE_EXACT_ALARM: that's a Play-Store-restricted "special use" permission reserved
         for alarm-clock/calendar-class apps and would likely be rejected on review for a photo
         cleanup app. SCHEDULE_EXACT_ALARM is the normal, runtime-revocable (API 31+) permission
         — granted automatically below API 31, requires a Settings redirect
         (ACTION_REQUEST_SCHEDULE_EXACT_ALARM) on 31+ if the user later revokes it. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".SwipyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- SwipyApplication supplies its own Configuration.Provider (HiltWorkerFactory) —
             WorkManager's default androidx.startup-based initializer must be removed or app
             startup crashes with "WorkManager is already initialized". -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
