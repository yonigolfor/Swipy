package com.swipy.domain.model

/** Mirrors the iOS FilterCategory cases 1:1 — see android/CLAUDE.md "Color Palette". */
enum class FilterCategory {
    All,
    Screenshots,
    ScreenRecordings,
    LargeVideos,
    BlurryPhotos,
    BurstPhotos,
}
