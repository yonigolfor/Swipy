<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".AlarmReceiver"
            android:exported="false" />

        <!-- Exact alarms don't survive a reboot — re-arms the weekly-cleanup/inactivity
             alarms (the two "OS-guaranteed forever" reminders) so a user who reboots their
             device without opening the app doesn't silently lose them. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
